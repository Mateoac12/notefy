export const popupScale = () => {
  return `
    @keyframes popupScale {
      0% {
        transform: scale(1.025);
      } 20% {
        transform: scale(.95);
      } 80% {
        transform: scale(1);
      }
    }
  `
}
